services:
  pond:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pond-api
    env_file:
      - .env
    ports:
      - "${PORT:-19100}:8000"
    restart: unless-stopped
    # For development with hot-reload, uncomment:
    # volumes:
    #   - ./src:/app/src:ro
    # command: ["uvicorn", "pond.api.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]